<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="kr.co.openkitchenIndex">
	<select id="recentlyClass" resultType="rdto">
		SELECT C_NO, C_NAME, C_SHORTINTRO, C_MAINSUMNAIL, C_TEMA, C_PRICE, T_AREA, T_EXPERT
		FROM CLASS C, TEACHER T
		WHERE C.T_NO = T.T_NO AND ROWNUM &lt; 6
		ORDER BY C_REGDATE DESC
	</select>
	
	<select id="recentlyTeacher" resultType="tcdto">
		SELECT T.T_NO, T_MAINSUMNAIL, T_DETAILSUMNAIL, T_HAVENICKNAME, T_EXPERTNAME, T_CAREER, T_SHORTINTRO, T_AREA, T_LONGINTRO, T_REGDATE, T_EXPERT, T_PERSTATUS, C_TEMA
		FROM CLASS C, TEACHER T
		WHERE C.T_NO = T.T_NO AND ROWNUM &lt; 6
		ORDER BY T_REGDATE DESC
	</select>
	
	<select id="recentlySpace" resultType="spdto">
		SELECT S_NO, S_MAINSUMNAIL, S_SHORTINTRO, S_NAME, S_TEMA, S_LOC
		FROM SPACE
		WHERE ROWNUM &lt; 6
		ORDER BY S_REGDATE DESC
	</select>
	<select id="ContentClass" resultType="rdto">
		SELECT C_NO, C_NAME, C_SHORTINTRO, C_MAINSUMNAIL, C_TEMA, C_PRICE, T_AREA, T_EXPERT
			FROM (SELECT ROWNUM NUM, C_NO, C_NAME, C_SHORTINTRO, C_MAINSUMNAIL, C_TEMA, C_PRICE, T_AREA, T_EXPERT
       			 FROM CLASS C, TEACHER T
				 WHERE C.T_NO = T.T_NO
      			 ORDER BY C_REGDATE DESC)
      	WHERE NUM BETWEEN 6 AND 13
    </select>
	<select id="ContentTeacher" resultType="tcdto">
		SELECT T_NO, T_MAINSUMNAIL, T_DETAILSUMNAIL, T_HAVENICKNAME, T_EXPERTNAME, T_CAREER, T_SHORTINTRO, T_AREA, T_LONGINTRO, T_REGDATE, T_EXPERT, T_PERSTATUS, C_TEMA
			FROM (SELECT ROWNUM NUM, T.T_NO, T_MAINSUMNAIL, T_DETAILSUMNAIL, T_HAVENICKNAME, T_EXPERTNAME, T_CAREER, T_SHORTINTRO, T_AREA, T_LONGINTRO, T_REGDATE, T_EXPERT, T_PERSTATUS, C_TEMA
				FROM CLASS C, TEACHER T
				WHERE C.T_NO = T.T_NO
				ORDER BY T_REGDATE DESC)
		WHERE NUM BETWEEN 6 AND 13
	</select>
	<select id="ContentSpace" resultType="spdto">
		SELECT S_NO, S_MAINSUMNAIL, S_SHORTINTRO, S_NAME, S_TEMA, S_LOC
			FROM(SELECT ROWNUM NUM, S_NO, S_MAINSUMNAIL, S_SHORTINTRO, S_NAME, S_TEMA, S_LOC
				FROM SPACE
				ORDER BY S_REGDATE DESC)
		WHERE NUM BETWEEN 6 AND 13
	</select>
	<select id="MoreClass" resultType="rdto" parameterType="int">
		SELECT C_NO, C_NAME, C_SHORTINTRO, C_MAINSUMNAIL, C_TEMA, C_PRICE, T_AREA, T_EXPERT
			FROM (SELECT ROWNUM NUM, C_NO, C_NAME, C_SHORTINTRO, C_MAINSUMNAIL, C_TEMA, C_PRICE, T_AREA, T_EXPERT
       			 FROM CLASS C, TEACHER T
				 WHERE C.T_NO = T.T_NO
      			 ORDER BY C_REGDATE DESC)
      	WHERE NUM BETWEEN 6 + ${count} AND 13 +${count}
    </select>
    <select id="MoreTeacher" resultType="tcdto" parameterType="int">
		SELECT T_NO, T_MAINSUMNAIL, T_DETAILSUMNAIL, T_HAVENICKNAME, T_EXPERTNAME, T_CAREER, T_SHORTINTRO, T_AREA, T_LONGINTRO, T_REGDATE, T_EXPERT, T_PERSTATUS, C_TEMA
			FROM (SELECT ROWNUM NUM, T.T_NO, T_MAINSUMNAIL, T_DETAILSUMNAIL, T_HAVENICKNAME, T_EXPERTNAME, T_CAREER, T_SHORTINTRO, T_AREA, T_LONGINTRO, T_REGDATE, T_EXPERT, T_PERSTATUS, C_TEMA
				FROM CLASS C, TEACHER T
				WHERE C.T_NO = T.T_NO
				ORDER BY T_REGDATE DESC)
      	WHERE NUM BETWEEN 6 + ${count} AND 13 +${count}
    </select>
    <select id="MoreSpace" resultType="spdto" parameterType="int">
		SELECT S_NO, S_MAINSUMNAIL, S_SHORTINTRO, S_NAME, S_TEMA, S_LOC
			FROM(SELECT ROWNUM NUM, S_NO, S_MAINSUMNAIL, S_SHORTINTRO, S_NAME, S_TEMA, S_LOC
				FROM SPACE
				ORDER BY S_REGDATE DESC)
      	WHERE NUM BETWEEN 6 + ${count} AND 13 +${count}
    </select>
</mapper>